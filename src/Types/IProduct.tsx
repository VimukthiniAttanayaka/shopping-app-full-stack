export interface IProduct {
    name: string,
    id?: string,
    quantity: string,
    price: string,
    discountedPrice: string,
    category: string,
    image: string,
    description?: string
}
